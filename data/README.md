# 每日 Token 归档

此目录保存当前 Codex 账号的每日 Token 快照，作为本机 SQLite 历史的可版本化备份。

- `daily-usage.json` 以 UTC 日期存储账户级 Token 总量；不是按设备累计的数据。
- 只归档已结束的 UTC 日期；当天数据始终直接读取 Codex，不写入此目录。
- 缺失日期表示来源未返回记录，不能推断为 0 Token。
- 不保存 Codex 登录令牌、设备名称或原始 SQLite 数据库。

多设备更新前应先同步远端 `main`，避免同时改写同一个 JSON 文件。后续自动同步会负责导出、提交和推送。
